name: Hello World Workflow

on:
  push:
    branches:
      - master

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
    - name: Say Hello
      run: echo "Hello, World from master branch!"
